# PRD: Customer Embedding App

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-07-03 |
| 상태 | Draft (프로토타입 단계, 실서비스 데이터/게이트웨이 미검증) |
| 저장소 | `customer-embedding` |
| 관련 문서 | `backend_api_spec.md`(API 명세), `data/customer_embedding_training_data_2000_segmented.xlsx`(학습 데이터) |

---

## 1. 개요

패션 커머스 고객의 클릭·구매·SNS 반응·인구통계 데이터를 임베딩·클러스터링해 **3개 고객 세그먼트**(성장형 핵심 / 안정형 주력 / 이탈위험 휴면)로 분류하고, 세그먼트별 페르소나·타겟팅 메시지·리텐션 액션을 제시하며, 캠페인을 실제로 발송할 수 있는 **운영용 웹 대시보드**다.

### 1.1 배경
- 기존에는 세그먼트 분석 결과가 노트북(Jupyter/Colab)과 정적 문서(Word)에만 존재해 마케팅 실무자가 바로 활용하기 어려웠다.
- 분석 결과를 실시간 조회·검색하고, 그 자리에서 캠페인을 발송까지 이어지는 도구가 필요했다.

### 1.2 문제 정의
| 문제 | 현재 상태 |
|---|---|
| 세그먼트 분석 결과가 노트북 밖으로 나오지 않음 | 웹 대시보드로 상시 조회 가능 |
| 개별 고객이 어느 세그먼트인지 즉시 확인 불가 | 고객 ID 검색 기능 |
| 세그먼트가 시간에 따라 어떻게 변하는지 추적 불가 | 월별 추이 + 이동 매트릭스 |
| 분석과 실행(캠페인 발송)이 분리되어 있음 | 대시보드 내 발송 버튼 → 실제 게이트웨이 연동 |

---

## 2. 목표

### 2.1 제품 목표
1. 세그먼트 현황·추이·개별 고객 정보를 한 화면에서 조회한다.
2. 세그먼트별 맞춤 메시지를 실제 채널(카카오 알림톡/SMS/이메일/푸시)로 발송한다.
3. 클러스터링 배치 결과(Python)와 서빙 계층(Node API)을 분리해, 분석 로직 변경이 서비스 배포에 영향을 주지 않게 한다.

### 2.2 성공 지표 (안)
| 지표 | 설명 |
|---|---|
| 세그먼트 조회 → 캠페인 발송까지 소요 시간 | 분석 결과 확인 후 5분 이내 발송 가능 |
| 이탈위험 휴면 세그먼트 크기 | 분기별 감소 추세 |
| 성장형 핵심 세그먼트 매출 증감률 | +20%대 유지 |
| 캠페인 발송 성공률 | 게이트웨이 연동 후 95% 이상 |

> 위 수치는 목표(안)이며, 실제 서비스 데이터 연동 후 베이스라인을 새로 측정해 조정이 필요하다.

---

## 3. 타겟 사용자

| 사용자 | 니즈 |
|---|---|
| CRM/리텐션 마케터 | 세그먼트별 특징 파악, 타겟 메시지 발송 |
| 마케팅 매니저 | 캠페인 승인, 발송 이력 확인 |
| 데이터 분석가 | 클러스터링 배치 결과 검증, 세그먼트 정의 조정 |

---

## 4. 범위

### 4.1 In Scope (이번 버전에 구현됨)
- 세그먼트 개요 / 추이 / 고객 검색 / 캠페인 관리 4개 화면
- 세그먼트별 페르소나(인구통계·취향·구매·행동 지표) 조회
- 카테고리 동시조회 기반 코디/상품 추천 인사이트
- 카카오 알림톡·SMS·이메일·푸시 발송 게이트웨이 (자격증명 미설정 시 자동 시뮬레이션)
- 배치 결과(JSON) 기반 API 서빙 구조
- Render 배포 가이드 (Static Site + Web Service)

### 4.2 Out of Scope (이번 버전에 없음 — 향후 과제)
- 실제 ERP/분석 DB 연동 (현재는 합성 데이터 + 시드 JSON)
- 로그인/권한 관리(RBAC) — 발송 권한 분리 없음
- 캠페인 발송 큐/워커 (현재 동기 처리, 최대 20명 상한)
- 세그먼트 발송 이력의 서버 측 영구 저장 (현재 브라우저 `localStorage`)
- 광고 플랫폼(Meta/TikTok) API 연동 — 광고 관리자 안내로 대체
- A/B 테스트, 메시지 성과(오픈율/클릭률) 트래킹

---

## 5. 핵심 기능 요구사항

### 5.1 세그먼트 개요
- `GET /api/v1/segments/summary` 결과를 카드 3개로 표시 (규모, 매출 추세, 대표 지표)
- 카드 클릭 시 상세 패널(인구통계·취향 프로필, 핵심 행동 지표) 전환

### 5.2 세그먼트 추이
- 최근 6개월 세그먼트 규모 라인 차트
- 월별 신규 편입 고객 수
- 지난달 → 이번달 세그먼트 이동 매트릭스 (유지/이동 인원)

### 5.3 고객 검색
- 고객 ID(부분 일치) 검색 → 세그먼트, 최근성, 구매빈도, 매출, 관심 카테고리 조회
- 검색 결과에서 해당 세그먼트의 캠페인 탭으로 바로 이동

### 5.4 캠페인 관리
- 세그먼트별 채널·카피 목록 표시
- 실제 발송 가능 채널(카카오/푸시)과 광고 채널(안내만) UI 구분
- 발송 확인 모달 → 게이트웨이 디스패치 → 결과(성공/실패/시뮬레이션/일부성공) 표시
- 발송 이력 테이블 (시각, 세그먼트, 채널, 메시지, 대상 수, 상태)

### 5.5 데이터 동기화
- "지금 동기화" 버튼으로 최신 세그먼트 데이터 재조회
- 백엔드 미응답 시 프론트엔드가 목업 데이터로 자동 폴백, 화면 하단에 연결 상태 표시

---

## 6. 시스템 아키텍처 (요약)

```
Python 클러스터링 배치잡 (PCA 임베딩 + K-means)
        │  결과 산출
        ▼
data/segment_*.json  ──▶  backend/server.js (Express API)  ──▶  frontend/index.html (정적 SPA)
        │                         │
        │                         └──▶ backend/gateways/* ──▶ 카카오(알리고) / SMS / 이메일(SendGrid) / 푸시(FCM)
        ▼
data/customer_embedding_training_data_2000_segmented.xlsx (원본 학습 데이터)
```

- **프론트엔드**: 정적 HTML/CSS/JS (프레임워크 없음), Render Static Site 배포
- **백엔드**: Node.js/Express, Render Web Service 배포, `data/*.json`을 읽어 API로 노출
- **클러스터링**: 별도 Python 배치(Colab 노트북)에서 PCA 임베딩 + Dense Fusion + K-means(k=3) 수행 후 JSON 산출
- **발송 게이트웨이**: 채널별 어댑터 모듈(`backend/gateways/`)로 분리, 자격증명 없으면 시뮬레이션 응답
- 상세 API 스펙은 `backend_api_spec.md` 참조

---

## 7. 데이터 요구사항

| 소스 | 현재 | 실서비스 전환 시 |
|---|---|---|
| 구매 이력, 고객 인구통계 | 합성 데이터 | ERP/커머스 DB (배치 복제 경유) |
| 웹/앱 세션·이벤트 로그 | 합성 데이터 | GA4/Amplitude 등 |
| SNS 광고 반응 | 합성 데이터 | 광고 플랫폼 API |
| 세그먼트 배정 결과 | 정적 JSON | DB 테이블(`segment_snapshot`) 매일 스냅샷 |

세그먼트 정의: VIP/일반/휴면 3개 그룹을 PCA+K-means로 재현하며, 실루엣 0.119 / 정답 세그먼트 대비 ARI 0.80 수준의 정합성을 확인함(합성 데이터 기준).

---

## 8. 비기능 요구사항

| 구분 | 요구사항 | 현재 상태 |
|---|---|---|
| 성능 | 캠페인 발송이 세그먼트 규모(수백~수천 명)에서도 타임아웃 없이 처리 | 미충족 — 현재 동기 처리 20명 상한, 큐 기반 비동기 처리 필요 |
| 보안 | 발송 자격증명(API 키)은 서버에만 보관, 프론트엔드 미노출 | 충족 (`.env`, Render Environment 탭) |
| 권한 | 발송 액션은 마케팅 매니저 이상만 가능 | 미충족 — 인증/RBAC 미구현 |
| 개인정보 | 고객 연락처(전화/이메일) 접근 권한·마스킹·보관기한 정책 | 미착수 — 실 데이터 연동 전 선행 필요 |
| 장애 대응 | 백엔드 장애 시 프론트엔드 정상 표시 | 충족 — 목업 자동 폴백 |
| 이식성 | 특정 대행사(알리고 등)에 종속되지 않는 구조 | 부분 충족 — 게이트웨이 모듈 교체로 대응 가능 |

---

## 9. 리스크 및 제약사항

- **데이터**: 현재 모든 지표·페르소나는 합성 데이터 기반이며, 실제 고객 행동과 다를 수 있다. 실 데이터 연동 전 캠페인 문구·타겟팅 로직 재검증 필요.
- **게이트웨이 미검증**: 카카오 알림톡/SendGrid/FCM 연동 코드는 각 공식 API 스펙대로 작성했으나 실제 자격증명으로 end-to-end 테스트를 하지 못했다. 실제 키 발급 후 1차 검증 필요.
- **확장성**: 동기 방식 발송은 세그먼트가 커지면(수백 명 이상) 타임아웃 위험이 있다. 큐(SQS/BullMQ) 도입 전까지는 소규모 캠페인에만 사용을 권장.
- **다중 사용자 환경 미지원**: 발송 이력이 브라우저별 `localStorage`에 저장되어 팀원 간 공유가 안 된다.
- **ERP 연동은 구매 이력만 커버**: 클릭 로그·SNS 반응은 별도 시스템 연동이 추가로 필요하다.

---

## 10. 로드맵 (안)

| 단계 | 내용 |
|---|---|
| **M0 (현재)** | 합성 데이터 기반 프로토타입, 발송 시뮬레이션 |
| **M1** | 실제 게이트웨이 자격증명 발급 및 end-to-end 발송 검증 |
| **M2** | 로그인/RBAC 도입, 발송 이력 DB 이전 |
| **M3** | ERP 구매 이력 연동 (야간 배치 → 분석 DB) |
| **M4** | GA4/광고 API 연동으로 클릭·SNS 데이터 실데이터 전환 |
| **M5** | 발송 큐 도입, 세그먼트 전체 규모 대상 캠페인 지원 |

---

## 11. 오픈 이슈

- [ ] 발송 이력을 서버 DB로 옮길지, 브라우저 저장을 유지할지 결정 필요
- [ ] RBAC 도입 시 인증 방식(사내 SSO vs 자체 로그인) 결정 필요
- [ ] 클러스터링 배치 주기(일 1회 vs 6시간) 확정 필요
- [ ] 카카오 알림톡 외 대체 채널(문자 대체발송) 우선순위 결정 필요
