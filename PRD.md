# PRD: Customer Embedding App (v3 — 최종본 기준)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v3.0 |
| 작성일 | 2026-09-14 |
| 상태 | 운영 중 (Render 배포, GitHub Actions 월간 자동화 가동) |
| 관련 문서 | `TRD.md`(기술 명세), `backend_api_spec.md`(API 상세), `SECURITY.md`(보안 점검), `STOREFRONT_INTEGRATION.md`(외부 연동 가이드) |

> **v3에서 고친 것**: v2의 시스템 아키텍처 다이어그램에서 `data/segment_*.json` 다음에
> `data/customer_embedding_training_data_2000_segmented.xlsx (원본 학습 데이터)`가 화살표로
> 이어져 있어, **xlsx가 json의 결과물인 것처럼 잘못 읽히는 문제**가 있었다. xlsx는 배치가
> **읽어들이는 입력**이지 산출물이 아니므로, §6 다이어그램을 입력/처리/출력이 명확히 구분되게
> 다시 그렸다. Neo4j Aura 관련 내용은 v2부터 포함되어 있었으나, 이번에 함께 재검증했다.

---

## 1. 개요

패션 커머스 고객의 클릭·구매·SNS 반응·인구통계 데이터를 임베딩·클러스터링해 **3개 고객
세그먼트**(성장형 핵심 / 안정형 주력 / 이탈위험 휴면)로 분류하고, 세그먼트별 페르소나·타겟팅
메시지·리텐션 액션을 제시하며, 캠페인을 실제로 발송할 수 있는 **운영용 웹 앱**이다. Neo4j
그래프 DB를 연동해 "이 고객과 비슷한 고객", "이 상품과 함께 보면 좋은 상품" 같은 개인화 탐색
기능까지 제공한다.

### 1.1 입력 데이터 (주어진 것으로 취급)

앱은 아래 스키마를 가진 xlsx 파일 하나를 유일한 원천 데이터로 받는다
(`data/customer_embedding_training_data_2000_segmented.xlsx`). 이 파일이 어떻게
만들어졌는지는 본 문서의 범위 밖이며, 앱은 이 스키마만 지켜지면 그대로 동작한다.

| 시트명 | 용도 |
|---|---|
| 세션_마스터 | 세션 단위 요약(시작·종료 시각, 디바이스, 최종 이벤트, 구매전환 여부) |
| 이벤트_로그 | 세션 내 개별 이벤트(순번, 유형, 카테고리, 상품ID, 체류시간 등) |
| 상품_마스터 | 상품ID·이름·카테고리·브랜드·가격 |
| 카테고리_동시조회_매트릭스 | 카테고리 간 동시조회 빈도 (12×12) — Neo4j 그래프용 |
| 상품_동시조회_TOP쌍 | 상품 간 동시조회 빈도 (상위 쌍) — Neo4j 그래프용 |
| 구매이력 | 구매 빈도·금액·월별 매출(12개월)·재구매 주기 등 — 클러스터링용 |
| SNS광고반응 | 채널·캠페인·해시태그별 반응 지표 — 클러스터링·캠페인 메시지 생성용 |
| 고객프로필(인구통계_취향) | 연령대·성별·직업·관심사·스타일·소비성향 등 — 클러스터링용 |

이 8개 시트는 **두 개의 서로 다른 파이프라인**에서 각각 다른 부분을 사용한다 (§6 참고).
- **클러스터링 배치**(`scripts/run_clustering.py`): 세션_마스터·이벤트_로그·구매이력·
  SNS광고반응·고객프로필 5개 시트 사용
- **그래프 DB 적재**(`scripts/export_graph_csvs.py`): 세션_마스터·이벤트_로그·상품_마스터·
  카테고리_동시조회_매트릭스·상품_동시조회_TOP쌍 5개 시트 사용 (구매이력의 월매출도 Customer
  노드 속성으로 함께 반영)

### 1.2 문제 정의

| 문제 | 해결 |
|---|---|
| 세그먼트 분석 결과가 노트북 밖으로 나오지 않음 | 웹 앱으로 상시 조회 가능 |
| 개별 고객이 어느 세그먼트인지 즉시 확인 불가 | 고객 ID 검색 기능 |
| 세그먼트가 시간에 따라 어떻게 변하는지 추적 불가 | 월별 추이 + 이동 매트릭스 (자동 누적) |
| 분석과 실행(캠페인 발송)이 분리되어 있음 | 앱 내 발송 버튼 → 실제 게이트웨이 연동 |
| "비슷한 고객", "함께 볼 상품" 같은 관계 기반 질문에 답할 수 없음 | **Neo4j Aura 그래프 DB 연동** |
| 매달 분석가가 수동으로 재계산·업로드해야 함 | GitHub Actions로 완전 자동화 |

---

## 2. 목표

### 2.1 제품 목표
1. 세그먼트 현황·추이·개별 고객 정보를 한 화면에서 조회한다.
2. 세그먼트별 맞춤 메시지를 실제 채널(카카오 알림톡/SMS/이메일/푸시)로 발송한다.
3. 그래프 기반 개인화(비슷한 고객, 상품 추천, 코디 위젯)를 제공한다.
4. 분석 배치(월 1회)와 서빙 계층을 분리하고, 배치는 사람 개입 없이 자동 실행한다.

### 2.2 성공 지표 (안)
| 지표 | 설명 |
|---|---|
| 세그먼트 조회 → 캠페인 발송까지 소요 시간 | 5분 이내 |
| 배치 재계산 자동화율 | 100% (수동 개입 없이 매달 자동 실행) |
| 이탈위험 휴면 세그먼트 크기 | 분기별 감소 추세 |
| 캠페인 발송 성공률 | 게이트웨이 연동 후 95% 이상 |

---

## 3. 타겟 사용자

| 사용자 | 니즈 |
|---|---|
| CRM/리텐션 마케터 | 세그먼트별 특징 파악, 타겟 메시지 발송, 개인화 상품 추천 |
| 마케팅 매니저 | 캠페인 승인, 발송 이력 확인, 추이 모니터링 |
| 데이터 분석가 | 배치 결과 검증, 그래프 탐색 쿼리 작성 |

---

## 4. 범위

### 4.1 In Scope (구현 완료)
- **세그먼트 개요**: 3개 세그먼트 카드 + 상세 프로필(인구통계·취향 1·2위, 구매·행동·SNS 지표 17종)
- **세그먼트 추이**: 월별 규모 추이(달력 비례 그래프, 데이터 공백 구간 시각적 표시), 신규 편입, 이동 매트릭스
- **고객 검색**: ID 검색(Enter 트리거), 세그먼트/구매지표 조회
- **캠페인 관리**: 세그먼트별 메시지(배치가 자동 생성) 발송, 발송 이력
- **코디 위젯 미리보기**: 상품 코디 추천 미리보기 + 쇼핑몰 연동 예시 코드
- **그래프 기반 개인화 (Neo4j Aura)**: 비슷한 고객 찾기, 협업 필터링 상품 추천, 코디 추천(CO_VIEWED_WITH 관계 기반)
- **실제 발송 게이트웨이**: 카카오 알림톡·SMS·이메일·푸시 (자격증명 없으면 자동 시뮬레이션)
- **월간 자동 배치**: GitHub Actions가 매달 1일 클러스터링 재계산 + 세그먼트 추이 자동 누적

### 4.2 Out of Scope (여전히 없음 — 향후 과제)
- 로그인/권한 관리(RBAC) — 발송 권한 분리 없음 (`SECURITY.md` 최우선 과제로 기록됨)
- 캠페인 발송 큐/워커 — 현재 동기 처리, 최대 20명 상한
- 발송 이력의 서버 측 영구 저장 — 현재 브라우저 `localStorage`
- 광고 플랫폼(Meta/TikTok) API 연동 — 광고 관리자 안내로 대체
- A/B 테스트, 메시지 성과(오픈율/클릭률) 트래킹
- 실제 ERP/실서비스 데이터 연동 — 현재는 제공된 xlsx 고정 입력

---

## 5. 핵심 기능 요구사항

### 5.1 세그먼트 개요
- 카드 3개(규모, 매출 추세, 대표 지표) → 클릭 시 상세 패널
- 상세 패널: 인구통계 8항목(관심사·스타일·소비성향은 1·2위 함께 표시) + 행동지표 17항목(체류시간, 관심 카테고리 top3, 이탈지점 top2, SNS 반응, 선호 채널 top3, 캠페인 유형 top2, 해시태그 top2)
- oneLiner(한 줄 요약)는 세그먼트의 실제 선호 채널 데이터를 반영해 매달 자동 생성됨

### 5.2 세그먼트 추이
- 월별 규모 라인 차트(달력 날짜에 비례한 x축, 데이터 공백 구간은 음영 표시)
- 실제 스냅샷이 부족한 초기 구간은 매출 데이터 기반 **근사치**로 채워지며, 화면에 "근사 추정치" 배너로 명시
- 이동 매트릭스(전월 → 당월 세그먼트 이동)

### 5.3 고객 검색
- 고객 ID 부분 일치 검색(Enter 또는 버튼으로 실행)
- 검색 결과에서 "비슷한 고객", "캠페인 보기" 액션 제공

### 5.4 캠페인 관리
- 세그먼트별 메시지는 **매달 배치가 실제 SNS 반응 데이터를 기반으로 자동 생성** (하드코딩 없음)
- 실제 발송 가능 채널(카카오/푸시)과 광고 채널(안내만) UI로 구분
- 발송 확인 모달 → 게이트웨이 디스패치 → 결과(성공/실패/시뮬레이션/일부성공) 표시 및 이력 기록

### 5.5 그래프 기반 개인화 (Neo4j Aura)
- 고객 검색 결과에서 "비슷한 고객 보기" → 겹치는 조회 상품 기준 유사 고객 목록
- 같은 화면에서 "추천할 상품" → 비슷한 고객이 봤지만 이 고객은 안 본 상품 → 개인화 메시지 즉시 발송
- 별도 탭에서 상품ID 입력 → 코디 추천 미리보기(카테고리·상품 동시조회 데이터 기반) + 실제 쇼핑몰 연동용 API 스펙 제공
- 그래프 DB가 설정되지 않았거나 응답에 실패해도 나머지 기능은 정상 동작 (§6, §8 참고)

### 5.6 데이터 동기화
- "지금 동기화" 버튼으로 최신 세그먼트 데이터 재조회
- 백엔드 미응답 시 프론트엔드가 **최소 placeholder**로 자동 전환(상세 페르소나 내용을 프론트엔드에 중복 저장하지 않음 — 배치 JSON이 유일한 원천)

---

## 6. 시스템 아키텍처 (요약)

**두 개의 독립된 파이프라인**이 같은 xlsx를 입력으로 받아 서로 다른 산출물을 만들고,
백엔드가 이 둘을 각각 다른 API로 서빙한다.

```
┌───────────────────────────────────────────────────────────────┐
│ 입력 (공통, 두 파이프라인 모두 읽기 전용으로 사용)                    │
│ data/customer_embedding_training_data_2000_segmented.xlsx      │
└───────────┬───────────────────────────────┬─────────────────────┘
            │                               │
            ▼                               ▼
┌─────────────────────────┐   ┌─────────────────────────────────┐
│ 파이프라인 A: 세그먼트 분류   │   │ 파이프라인 B: 그래프 DB 적재        │
│ (GitHub Actions, 매달 자동) │   │ (사람이 필요할 때 수동 실행)         │
│                          │   │                                 │
│ scripts/run_clustering.py│   │ scripts/export_graph_csvs.py    │
│  PCA 임베딩 + K-means     │   │  → data/graph/*.csv 생성          │
│  → data/segment_*.json   │   │ scripts/import_to_neo4j.cypher  │
│    갱신                   │   │  → Neo4j Aura에 MERGE로 적재       │
│  → git commit & push     │   │                                 │
└───────────┬───────────────┘   └───────────────┬──────────────────┘
            │                                   │
            ▼                                   ▼
┌───────────────────────────────────────────────────────────────┐
│ backend/server.js (Node/Express, Render Web Service)            │
│  ├─ data/*.json require() 후 세그먼트 API로 서빙                    │
│  ├─ backend/gateways/* → 카카오·SMS·이메일·푸시 실제 발송             │
│  └─ backend/graph.js → Neo4j Aura에 Cypher 쿼리 (비슷한 고객·추천·코디)│
└───────────┬───────────────────────────────────────────────────┘
            │ fetch (CORS 허용)
            ▼
┌───────────────────────────────────────────────────────────────┐
│ frontend/index.html (정적 SPA, Render Static Site)                │
│  백엔드 실패 시 최소 placeholder로 자동 폴백                          │
└───────────────────────────────────────────────────────────────┘
```

**왜 두 파이프라인으로 나눴는가**
- 세그먼트 분류(A)는 "정기적으로, 무인으로" 갱신되어야 해서 GitHub Actions 스케줄에 올렸다.
- 그래프 DB 적재(B)는 원본 데이터가 바뀔 때만 필요해서, 무리하게 자동화하지 않고 사람이
  필요한 시점에 두 스크립트를 순서대로 실행하는 방식으로 남겨뒀다 (Neo4j Aura 비밀번호는
  분실 시 재발급이 아니라 인스턴스 재생성이 필요해, 완전 자동화보다 통제된 수동 실행이 더
  안전하다고 판단함).

상세 구성도·API 스펙은 `TRD.md`, `backend_api_spec.md` 참고.

---

## 7. 비기능 요구사항

| 구분 | 요구사항 | 현재 상태 |
|---|---|---|
| 성능 | 배치가 매달 무인으로 완료 | 충족 (로컬·CI 실행 검증됨) |
| 성능 | 캠페인 발송이 대규모 세그먼트에서도 타임아웃 없이 처리 | 미충족 — 동기 처리 20명 상한, 큐 전환 필요 |
| 보안 | 발송 자격증명은 서버 환경변수에만 존재 | 충족 |
| 보안 | API 인증/RBAC | 미충족 (`SECURITY.md` Critical #1) |
| 데이터 정합성 | 입력 데이터의 논리적 모순(예: 최근구매일 이후 매출 존재) 없음 | 충족 — 발견 즉시 수정 완료 (`scripts/fix_purchase_consistency.py`) |
| 장애 대응 | 백엔드/그래프 DB 장애 시 프론트엔드 정상 표시 | 충족 — 목업 자동 폴백 |
| 접근성/반응형 | 모바일 화면에서 가로 스크롤 없이 표시 | 충족 |

---

## 8. 리스크 및 제약사항

- **입력 데이터 의존**: 앱의 모든 분석은 제공된 xlsx 스키마에 강하게 결합되어 있다. 스키마가
  바뀌면 `run_clustering.py`, `export_graph_csvs.py`의 컬럼 매핑을 함께 수정해야 한다.
- **그래프 DB 적재는 수동**: xlsx의 동시조회 데이터가 바뀌어도 Neo4j에 자동 반영되지 않는다
  — `export_graph_csvs.py` 실행 → GitHub 커밋 → `import_to_neo4j.cypher` 수동 실행이
  필요하다. 자동화하지 않은 것은 설계 선택(§6)이지만, 갱신을 잊으면 그래프 DB가 최신 xlsx와
  어긋날 수 있다.
- **Neo4j Aura Free 인스턴스는 계정당 1개**: 비밀번호를 잊으면 인스턴스를 삭제하고 새로
  만들어야 하며, 재적재가 필요하다.
- **추이 그래프의 근사 구간**: 실제 월별 스냅샷이 12개월 쌓이기 전까지, 과거 구간은 매출
  데이터만으로 근사한 값이다.
- **게이트웨이 미검증**: 카카오 알림톡·SendGrid·FCM 연동 코드는 스펙대로 작성했으나 실제
  자격증명으로 end-to-end 검증은 되지 않았다.
- **인증 부재**: 발송 API를 포함한 모든 엔드포인트가 인증 없이 열려 있다 (`SECURITY.md` 참고).
- **동기 발송 한계**: 세그먼트 전체(수백~수천 명) 발송 시 큐 없이는 타임아웃 위험이 있다.

---

## 9. 로드맵 (안)

| 단계 | 내용 |
|---|---|
| **M0 (완료)** | 세그먼트 조회·발송 시뮬레이션·그래프 개인화·월간 자동 배치까지 구축 |
| **M1** | 실제 게이트웨이 자격증명 발급 및 end-to-end 발송 검증 |
| **M2** | 로그인/RBAC 도입, CORS 제한, 입력값 검증 (`SECURITY.md` 상위 우선순위 반영) |
| **M3** | 발송 이력 DB 이전, 발송 큐 도입 |
| **M4** | 그래프 DB 적재(파이프라인 B)도 GitHub Actions로 자동화할지 검토 |
| **M5** | 실제 ERP/서비스 데이터로 입력 전환 |
| **M6** | 12개월 실제 스냅샷 축적 완료 → 근사 추이 구간 완전 대체 |

---

## 10. 오픈 이슈

- [ ] 발송 이력을 서버 DB로 옮길지, 브라우저 저장을 유지할지 결정 필요
- [ ] RBAC 도입 시 인증 방식(사내 SSO vs 자체 로그인) 결정 필요
- [ ] 그래프 DB 적재(파이프라인 B) 자동화 여부 결정 필요
- [ ] 실제 입력 데이터(ERP 등)로 전환할 목표 시점 결정 필요
- [ ] 카카오 알림톡 외 대체 채널(문자 대체발송) 우선순위 결정 필요
